import { canonicalToDisplayDate } from "../utils/dateUtils"; export default function ChangedDateCell({previous,current}:{previous:string|null;current:string|null}){const p=canonicalToDisplayDate(previous), c=canonicalToDisplayDate(current); if((previous??null)===(current??null))return <span className="date-change date-change__static">{c||"—"}</span>; return <span className="date-change"><span className="date-change__prev">{p||"—"}</span><span className="date-change__arrow">→</span><span className="date-change__next">{c||"—"}</span></span>}
