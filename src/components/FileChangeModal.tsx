import FileUploadCard from "./FileUploadCard";
export default function FileChangeModal(p:{open:boolean;plannerFileName:string|null;masterFileName:string|null;onPickPlanner:(file:File)=>void;onPickMaster:(file:File)=>void;onClose:()=>void}){ if(!p.open)return null; return <div className="modal-backdrop" onClick={p.onClose}><div className="modal" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><div className="modal__header"><h2 className="modal__title">Cargar / cambiar archivos</h2><button className="btn btn--ghost" onClick={p.onClose}>×</button></div><div className="modal__body"><FileUploadCard title="Excel de Planner (actual)" currentFileName={p.plannerFileName} onFilePicked={p.onPickPlanner}/><FileUploadCard title="Excel maestro" currentFileName={p.masterFileName} onFilePicked={p.onPickMaster}/></div><div className="modal__footer"><button className="btn" onClick={p.onClose}>Cerrar</button></div></div></div>}
