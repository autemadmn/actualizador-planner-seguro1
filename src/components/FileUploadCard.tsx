import { useRef, useState } from "react";
export default function FileUploadCard({title,hint,currentFileName,onFilePicked}:{title:string;hint?:string;currentFileName?:string|null;onFilePicked:(file:File)=>void}){const [drag,setDrag]=useState(false); const ref=useRef<HTMLInputElement|null>(null); const icon=`${import.meta.env.BASE_URL}excellogo.png`; const pick=(f?:File|null)=>{if(f)onFilePicked(f)}; return <div className={`upload-card${drag?" upload-card--dragging":""}`} onClick={()=>ref.current?.click()} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);pick(e.dataTransfer.files?.[0])}} role="button" tabIndex={0}><img src={icon} alt="" className="upload-card__icon"/><p className="upload-card__title">{title}</p><p className="upload-card__hint">{hint??"Arrastra el archivo aquí o haz clic para seleccionarlo"}</p>{currentFileName&&<p className="upload-card__current">Actual: {currentFileName}</p>}<input ref={ref} type="file" accept=".xlsx" hidden onChange={e=>pick(e.target.files?.[0])}/></div>}
