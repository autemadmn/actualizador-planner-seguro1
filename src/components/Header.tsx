export interface HeaderProps { canCreateMaster: boolean; onCreateMaster: () => void; onOpenFileModal: () => void; }
export default function Header(p:HeaderProps){ const logo=`${import.meta.env.BASE_URL}power-electronics-transparente.webp`; return <header className="app-header"><div className="app-header__brand"><img src={logo} alt="Power Electronics" className="app-header__logo"/><h1 className="app-header__title">Actualizador Planner</h1></div><div className="app-header__actions"><button className="btn btn--primary" onClick={p.onCreateMaster} disabled={!p.canCreateMaster}>Crea Excel Maestro Actualizado</button><button className="btn" onClick={p.onOpenFileModal}>Cambiar archivos</button><button className="btn btn--ghost" aria-label="Más opciones">⋯</button></div></header>}
